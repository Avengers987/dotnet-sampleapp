name: .NET CI Pipeline
#details
on:
  push:
  pull_request:
    branches: 
     - main
     - feature1
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: read
     
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore
        run: restore WebApp.sln

      - name: Build
        run: dotnet build --no-restore WebApp.sln 

      - name: Test
        run: dotnet test --no-build WebApp.sln

      - name: Publish
        run: dotnet publish WebApp.sln -c Release -o publish

      - name: Upload publish artifacts
        uses: actions/upload-artifact@v4
        with:
          name: webapp-publish
          path: publish/
